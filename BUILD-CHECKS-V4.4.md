# Build checks — V4.4 image positioning refresh

Completed checks:

- TypeScript/TSX syntax parsed successfully across the project.
- Duplicate object-property scan passed.
- All referenced `/images/...` files were checked and exist.
- All 44 optimised image-refresh files are referenced by the website.
- Homepage H1 confirmed as `Fire Alarm, CCTV & Intruder Systems`.
- Homepage hero uses one shared cropped image on desktop and mobile, with the left-side car excluded.
- Responsive image-gallery CSS includes desktop, tablet and mobile layouts.

The full `next build` could not be run in this environment because the npm registry repeatedly failed DNS resolution while dependencies were being installed. No claim is made that a full production build completed here.
